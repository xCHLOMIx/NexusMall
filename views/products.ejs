<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="Styles/products.css">
    <title>NexusMall</title>
</head>
<body>
    <nav>
        <div class="logo"><a href="/"><img src="Images/Exclusion 1.png" alt="" id="logo"></div>
        <div class="links">
            <a href="/home"><img src="Icons/home.png" id="other"></a>
            <a href="/products"><img src="Icons/browse.png" id="home"></a>
            <a href="/orders"><img src="Icons/account.png" id="other"></a>
        </div>
        <div class="logout"><img src="Icons/logout.png" id="logout"></div>
    </nav>
    <div class="content">
        <div class="product">
            <h1>In Stock:</h1>
        <% for( let i = 0; i < product.length; i++) { 
            if(i % 3 === 2) {%>
                <div class="showcase" id="b1">
                    <div class="img"><img
                            src="data:<%= product[i].image.contentType %>;base64,<%= product[i].image.data.toString('base64') %>"
                            alt="">
                    </div>
                    <div class="text">
                        <h1>
                            <%= product[i].productName %>
                        </h1><br>
                        <b>PRICE:</b>
                        <p>$<%= product[i].unitPrice %>
                        </p>
                        <p id="desc">
                            <%= product[i].productDescription %>
                        </p>
                        <a href="/<%- product[i]._id  %>">
                            <button id="no"><img src="Icons/clipboard.png" alt="Order NOW!"></button>
                        </a>
                    </div>
                </div>
            <% } else { %>
                <div class="showcase">
                    <div class="img"><img
                            src="data:<%= product[i].image.contentType %>;base64,<%= product[i].image.data.toString('base64') %>"
                            alt="">
                    </div>
                    <div class="text">
                        <h1>
                            <%= product[i].productName %>
                        </h1><br>
                        <b>PRICE:</b>
                        <p>$<%= product[i].unitPrice %>
                        </p>
                        <p id="desc">
                            <%= product[i].productDescription %>
                        </p>
                        <a href="/<%- product[i]._id  %>">
                            <button><img src="Icons/clipboard.png" alt="Order NOW!"></button>
                        </a>
                    </div>
                </div>
        <% }}; %>
            </div>
        </div>
    </div>
</body>
</html>